[pytest]

markers= 
    smoke: pruebas esenciales que validan la funcionalidad base
    regression: pruebas que aseguran que no se rompieron funcionalidades existentes
    negative: pruebas negativas con inputs o comportamientos no válidos
    ui: pruebas enfocadas en validaciones visuales o de interfaz
    performance: pruebas enfocadas en tiempo de respuesta o rendimiento